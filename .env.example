# Copy to .env at project root (required by docker/docker-compose.yaml)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=fraud_system

# Backend (Phase 1+) — also set in backend/.env
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/fraud_system
